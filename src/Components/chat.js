import React from "react";
import "../styles/chat.css"

const Chat = () => {
  return (
    <div className="p-8">
      <h1 className="text-3xl font-bold">Chat Page</h1>
      <p className="mt-4">This is the chat page where users can interact with the chatbot.</p>
    </div>
  );
};

export default Chat;
